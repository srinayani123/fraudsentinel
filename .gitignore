# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/

# IDEs
.vscode/
.idea/
*.swp
.DS_Store

# Environment
.env
*.env.local

# Data â€” DO NOT commit raw data, only commit `data/samples/` and `data/fraud_cases/`
data/raw/
data/processed/
data/features/
*.zip
*.tar.gz

# MLflow
mlruns/
mlartifacts/

# Logs
*.log
logs/

# Kaggle
.kaggle/

# Streamlit
.streamlit/secrets.toml

# Jupyter
.ipynb_checkpoints/

# Models â€” Allow committing trained model artifacts so the demo works out-of-the-box
# (Comment out the next line if your models are too big for git)
# models/

# Spark
spark-warehouse/
metastore_db/
derby.log

# OS
Thumbs.db

# Diagnostic scripts (developer-only debugging tools)
diag_*.py


# Investigation cache (runtime artifact, not source code)
data/cache/

# Line endings — standardize on LF in git, let Windows convert to CRLF on checkout
* text=auto eol=lf

# Investigation cache (runtime artifact, regenerated on use)
data/cache/

# Backup files
*.backup
*.old

# Line endings — standardize on LF in git
* text=auto eol=lf
